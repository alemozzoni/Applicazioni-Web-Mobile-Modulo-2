# Copy to .env and customize

# --- JWT (REQUIRED) ---
JWT_SECRET=ks83jD83jd93Jd82jd83Jd92jd83Jd83J
JWT_REFRESH_SECRET=98dj29DJS93jd82jd93jd82jd92jd83jd

# --- Database (PostgreSQL) ---
# These defaults match docker-compose.yml
DB_NAME=jbudget
DB_USER=jbudget
DB_PASS=@Data2004
# In Docker, DB_HOST is forced to "db" automatically.
# If you run the API locally (without Docker), set DB_HOST=localhost
DB_HOST=localhost
DB_PORT=5432
# Set to "true" if you terminate TLS to Postgres (typical on managed DBs)
DB_SSL=false

# If you want to lock down CORS (API), set to your site origin, e.g. https://example.com
CORS_ORIGIN=*
